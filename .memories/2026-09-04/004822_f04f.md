---
id: checkpoint_f04fbd58
timestamp: 2026-09-04T00:48:22.854Z
tags:
  - agy-cli
  - security
  - review-fixes
  - timeout
git:
  branch: main
  commit: 682c00e
  files:
    - skills/agy-cli/SKILL.md
    - skills/claude-cli/SKILL.md
    - skills/codex-cli/SKILL.md
    - skills/grok-cli/SKILL.md
    - skills/security-review/SKILL.md
    - tests/agy-cli-docs.test.mjs
actor:
  harness: antigravity-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Addressed Codex code review findings for `agy-cli` and reviewer recipes
type: checkpoint
impact: Hardens agy-cli and reviewer skills against early timeouts, payload
  leaks, and policy ambiguities.
evidence:
  - npm test passed with 379 tests (0 failures)
  - ./scripts/bump-version.sh --audit passed cleanly
confidence: 5
---

## Addressed Codex code review findings for `agy-cli` and reviewer recipes

Resolved all 4 findings from Codex's review of the `agy-cli` integration:

- **WHAT:**
  1. Updated `skills/agy-cli/SKILL.md` to pass `--print-timeout 30m` in the review invocation and documented the 5-minute default vs 30-minute failsafe contract.
  2. Fixed raw `PAYLOAD_FILE` leak: removed `PAYLOAD_FILE` immediately following successful `redact-outbound` execution and included it in the `prepare-review-artifact` failure cleanup branch across `agy-cli`, `codex-cli`, `claude-cli`, and `grok-cli`.
  3. Clarified `skills/security-review/SKILL.md` dispatch policy check so that `Reviewer choices permitted:` explicitly governs pre-merge review dispatches while standalone reviewer CLIs check `Allowed providers:`.
  4. Simplified `skills/agy-cli/SKILL.md` pre-flight check to `agy models`, eliminating unused `AGY_BIN` fallback mismatch.
  5. Added guard tests in `tests/agy-cli-docs.test.mjs` verifying `--print-timeout 30m` and `PAYLOAD_FILE` cleanup.
- **WHY:** Review depth and security guarantees: prevent unredacted diffs leaking on disk, ensure `agy` does not exit at 5 minutes during deep reviews, clarify policy checks, and maintain consistent PATH-based CLI preflight checks.
- **HOW:** Targeted documentation, recipe, and test updates across 6 files.
- **IMPACT:** Hardens the review pipeline and ensures full compliance with Razorback's failsafe timeout and redaction requirements.
