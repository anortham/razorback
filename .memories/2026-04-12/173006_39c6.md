---
id: checkpoint_39c61cea
timestamp: 2026-04-12T17:30:06.724Z
tags:
  - release
  - v0.7.0
  - opencode
  - dual-harness
git:
  branch: main
  commit: c45c527
  files:
    - .claude-plugin/plugin.json
    - .julie/.gitignore
summary: "v0.7.0 release: opencode dual-harness support shipped"
type: checkpoint
impact: Razorback now supports opencode with one-line install, no symlinks.
  Existing Claude Code functionality unchanged.
next: Commit version bump, tag v0.7.0, push main + tag to origin.
---

## v0.7.0 release: opencode dual-harness support shipped

All 4 tasks from `docs/plans/2026-04-12-opencode-dual-harness.md` complete and integration-verified. Bumping plugin version from 0.6.1 to 0.7.0 and pushing main + tag.

### What shipped

- Modernized `.opencode/plugins/razorback.js`: `config` hook + `messages.transform` (dropped stale `system.transform`), string-replaces Execution Model section for opencode, idempotent bootstrap injection on first user message.
- New install UX: `.opencode/INSTALL.md` (one-line `opencode.json` plugin entry), root `package.json` (enables `bun install`), `AGENTS.md` → `CLAUDE.md` symlink.
- `subagent-driven-development` un-deprecated and rewritten as opencode execution primary (inline review by lead, Julie-first, updated example workflow and red flags).
- `CLAUDE.md` + `README.md` reflect per-harness execution (team-driven on CC, subagent-driven on OC).
- `docs/README.opencode.md` rewritten 276 → 140 lines, drops all symlink install paths.

### Git history (7 commits on main before bump)

- 49527fb design spec
- 820fc5f implementation plan
- d5a6c7b Task 1 (opencode plugin + install infra)
- 6384c9e Task 3 (CLAUDE.md + README)
- d159488 Task 2 (subagent-driven revival)
- 21f0b59 Task 4 (opencode docs rewrite)
- c45c527 Task 1 follow-up (dead-code cleanup — couldn't amend due to race with other teammates landing first)

### Outstanding follow-ups (not blocking)

- `skills/subagent-driven-development/spec-reviewer-prompt.md` and `code-quality-reviewer-prompt.md` are still written in second-person "you are a reviewer" style even though they're no longer dispatched — should be rewritten as lead checklists in first-person or imperative form. Flagged by skill-reviver teammate.
- README "After Installation" section (lines 70-76) still describes only Claude Code flow (SessionStart hook, Agent Teams). Opencode install subsection above points to detailed docs so it's acceptable but slightly Claude-Code-centric.

### Process observation

Team-driven worked well here. Four teammates, disjoint files, all reported DONE in ~2 minutes of wall time. Only intervention needed: (1) a mid-flight message to project-docs when CLAUDE.md was updated externally during spawn, (2) one dead-code cleanup fix. opencode-infra's amend-race recovery was handled cleanly by the teammate.
