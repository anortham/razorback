---
id: checkpoint_8c84b77e
timestamp: 2026-09-20T17:49:32.171Z
tags:
  - codex-cli
  - bug-fix
  - review
git:
  branch: main
  commit: "1e86073"
  files:
    - skills/codex-cli/SKILL.md
    - tests/codex-sandbox-preflight.test.mjs
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: codex-cli review recipes now pass --skip-git-repo-check
briefId: inner-loop-test-isolation
type: checkpoint
evidence:
  - "npm test: 438 pass, 0 fail"
---

## codex-cli review recipes now pass --skip-git-repo-check

**WHAT:** Added `--skip-git-repo-check` to the Code Review Step 3 and Adversarial Review Step 3 dispatch lines in `skills/codex-cli/SKILL.md`, and expanded the flag note in the Delegate section. Added a guard test in `tests/codex-sandbox-preflight.test.mjs` that requires the flag on every dispatch aimed at `$REVIEW_ROOT`.

**WHY:** Users in other projects hit "Codex refused the git-free review tree" every time. The review recipes point codex at the exported review tree, which has no `.git`, so codex rejected the arguments before any model turn. The pre-merge-review codex prompt already carried the flag; the codex-cli skill did not.

**HOW:** Root cause fix in the skill recipes, not a retry note. Test matches the two `codex-exec` + `-C "$REVIEW_ROOT"` lines and asserts the flag.

**IMPACT:** codex-cli reviews run on the first try. Full `npm test` passes (438 tests).
