---
id: checkpoint_bafc40a4
timestamp: 2026-07-03T12:53:48.308Z
tags:
  - razorback
  - release-prep
  - version-bump
  - codex-plugin
  - local-main
git:
  branch: main
  commit: 7e7481e
  files:
    - .claude-plugin/marketplace.json
    - .claude-plugin/plugin.json
    - .codex-plugin/plugin.json
    - .cursor-plugin/plugin.json
    - gemini-extension.json
    - package.json
summary: Prepared Razorback 0.20.0 release locally
briefId: agent-tier-delegation-gate-policy-feedback
type: checkpoint
context: After fast-forward merging the Codex parallelism/plugin branch into
  local main, the user asked to prep a new release without pushing or
  publishing.
impact: Moves the completed feature branch onto local main and prepares the next
  version without crossing push/release approval boundaries.
evidence:
  - node --test tests/*.test.mjs => 73 pass, 0 fail after merge and after
    version bump
  - ./scripts/bump-version.sh --audit => six declared version files in sync at
    0.20.0, no undeclared refs
  - git diff --check => exit 0
  - git worktree list => only /Users/murphy/source/razorback remains
next: Commit the 0.20.0 release prep locally, then run post-commit package smoke
  against HEAD.
confidence: 5
---

## Prepared Razorback 0.20.0 release locally

### WHAT

Fast-forward merged `codex/codex-parallelism-plugin-implementation` into local `main`, removed the now-merged linked worktree/branch, and bumped all declared version surfaces from `0.19.0` to `0.20.0`.

### WHY

The merged work adds Codex plugin distribution, packaging tests, stronger parallel subagent dispatch guidance, and simplifies Razorback policy by removing the separate `RAZORBACK.md` model-routing surface. This is release-sized behavior, so the local release prep uses a minor version bump.

### HOW

- Verified both worktrees were clean before merging.
- Fast-forwarded `main` from `383fcc3` to `7e7481e`.
- Removed `/Users/murphy/source/razorback/.worktrees/codex-parallelism-plugin-implementation` and deleted the merged local branch.
- Ran `./scripts/bump-version.sh 0.20.0`, which updated `package.json`, Claude/Cursor/Codex/Gemini manifests, and the Claude marketplace version field.
- Confirmed there is no tracked changelog or `RELEASE-NOTES.md`; release prep is manifest/version based in this repo.

### IMPACT

Local `main` is ready for a `0.20.0` release-prep commit. No tag, push, publish, or release action has been performed.
