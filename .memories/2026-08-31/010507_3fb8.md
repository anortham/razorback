---
id: checkpoint_3fb8d4e8
timestamp: 2026-08-31T01:05:07.290Z
tags:
  - linear-hardening
  - implementation-start
  - grok-review
git:
  branch: codex/linear-hardening
  commit: ddc519f
  files:
    - docs/plans/2026-08-30-linear-hardening.md
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
type: checkpoint
impact: Creates a single release-candidate branch while keeping release actions
  behind explicit approval.
evidence:
  - Existing linear-hardening branch HEAD ddc519f contains the approved design.
  - Grok fix commit 48e4518 passed focused 8/8 and full repository 284/284
    verification before integration.
  - Current linear-hardening dirty state is limited to the approved
    implementation plan and its Goldfish checkpoints.
next: Commit plan artifacts, cherry-pick 48e4518, run baseline verification,
  then dispatch plan Tasks 1 and 2.
---

## WHAT

The Linear hardening design and implementation plan are approved and execution is starting in `/home/murphy/.config/razorback/worktrees/razorback/linear-hardening` on `codex/linear-hardening`.

The user also directed that verified Grok sandbox-guidance commit `48e4518` be merged into this branch and that the completed implementation receive one bounded Grok review.

## WHY

Main has only the backlog-pointer commit beyond v0.34.1. The real fixes remain split between the tested Grok branch and the approved but unimplemented hardening plan.

## HOW

Commit the approved plan artifacts first, cherry-pick `48e4518`, verify the resulting baseline, then execute plan Tasks 1 and 2 in parallel with Luna implementers, followed by serialized Tasks 3 and 4. The lead owns inline review, branch/security gates, the bounded Grok campaign, and durable Linear/Goldfish state.

The planned repository policy remains `anthropic, openai` with reviewer choices `codex, claude`. The user's direct request explicitly authorizes the one Grok/xAI review for this run; record that one-off authorization in the final report rather than silently changing the durable policy.

## IMPACT

This consolidates the release work on one branch and preserves the approved security policy while honoring the explicit one-time Grok review request. No push, tag, release, or merge to main is authorized.
