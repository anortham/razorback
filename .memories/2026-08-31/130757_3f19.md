---
id: checkpoint_3f19f3e8
timestamp: 2026-08-31T13:07:57.415Z
tags:
  - release
  - 0.35.0
  - linear-hardening
  - recipe-execution
  - cleanup
git:
  branch: main
  commit: a905234
  files:
    - .claude-plugin/marketplace.json
    - .claude-plugin/plugin.json
    - .codex-plugin/plugin.json
    - .cursor-plugin/plugin.json
    - package.json
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Merged Linear hardening and cut 0.35.0
type: checkpoint
impact: Ships the hardened reviewer path and the recipe fix together. The *-cli
  skills now define their anchor before use and state the one-shell rule, so a
  normal review dispatch no longer sends an empty prompt.
evidence:
  - "npm test on merged main: 353/353 passing at a905234"
  - "./scripts/bump-version.sh --audit: all five manifests in sync at 0.35.0, no
    undeclared references"
  - "gitleaks detect --redact=100: no leaks, 304 commits scanned"
  - "git diff --check: clean"
next: Push main with the tag and publish the GitHub release. The corrected Grok
  artifact path is still unexercised against xAI; a fresh sandbox-off campaign
  needs explicit user approval.
unknowns:
  - Whether Grok 4.6 completes a review through the reviewer-local artifact path
    on a fresh campaign
---

## Merged Linear hardening and cut 0.35.0

### WHAT

Merged `codex/linear-hardening` into `main` as `a905234` (`--no-ff`, matching the repo's merge convention). Bumped all five manifests to 0.35.0. Removed the two redundant worktrees and their branches, and deleted a stray 13 MB ImageMagick PostScript file that a shell brace typo had created in the branch worktree.

### WHY

The user reported repeated failures using the `*-cli` skills to get a review done, after contentious back-and-forth with two agents. The hardening work was sound; the reviewer recipes were simply not runnable. Both causes are now fixed and guarded, so the whole set ships together.

### HOW

- `feat/bounded-review-campaigns` was already merged into `main`. Its worktree was stale.
- `codex/grok-sandbox-guidance` held one commit whose diff was byte-identical to `e8a92ef` inside the hardening branch. Redundant.
- Miller registry entries for both removed roots were retired. The second retirement refused once with `stale_plan` and succeeded on retry, reclaiming 13 MB of sidecar files.

### IMPACT

One worktree remains besides `main`: `codex/linear-hardening`, kept as the merged source branch. The release is gate-clean and ready to tag.
