---
id: checkpoint_dcaa2deb
timestamp: 2026-08-31T03:24:04.849Z
tags:
  - linear-hardening
  - branch-gate
  - grok-review
  - blocked
git:
  branch: codex/linear-hardening
  commit: 2bd6328
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Linear hardening passes the final local gate; external review needs a
  new campaign
type: checkpoint
impact: All local gates pass; only the explicitly required Grok review remains
  blocked on fresh authorization.
evidence:
  - "npm test: 344/344 passing at 2bd6328"
  - "./scripts/bump-version.sh --audit: 0.34.1 synchronized; no undeclared
    references"
  - "gitleaks detect --redact=100: no leaks; 303 commits scanned"
  - "git diff --check: pass"
  - worktree clean before this checkpoint
next: Commit this checkpoint, report the clean state, and ask the user for
  explicit approval to open one new Grok sandbox-off review campaign against the
  corrected artifact path.
unknowns:
  - Whether Grok 4.6 will follow the artifact path and complete the review on a
    fresh campaign.
---

## Linear hardening passes the final local gate; external review needs a new campaign

### WHAT

Committed the large-review artifact transport correction as `2bd6328ee47303e61229af018294d3d658704304`. The complete Linear hardening branch now passes the full repository and security gates.

### WHY

The first user-authorized Grok campaign exhausted its two invocations on planning placeholders. The code now fixes the transport cause, but campaign discipline forbids a third call under the same authorization.

### HOW

The lead reviewed the worker's implementation, found and corrected small-prompt instruction loss, unredacted focus text, small-payload validation bypass, and a copied variable-name drift. The final helper and all reviewer recipes use the same 128 KiB contract.

### IMPACT

Local implementation is release-gate clean. A completed Grok review still does not exist. Starting a fresh sandbox-off Grok campaign requires explicit user approval because the prior campaign closed at 2/2 invocations and the repository policy does not generally allow xAI.
