---
id: checkpoint_ecc60f93
timestamp: 2026-08-07T14:36:45.367Z
tags:
  - release
  - 0.27.0
  - security-pass
  - security-lane
  - pre-merge-review
  - milestone
  - worktree-cleanup
git:
  branch: main
  commit: 18f3034
summary: Published v0.27.0 — release live, session milestone closed
briefId: harness-descope-2026-07-audit-remediation
type: checkpoint
evidence:
  - gh release at https://github.com/anortham/razorback/releases/tag/v0.27.0
  - npm test 196/196 on main @ 18f3034
next: "User decisions pending: hospital CI/DevOps PR reviewer, policy-block
  dogfood in razorback CLAUDE.md, stale brief resolution"
---

## Published v0.27.0 — release live, session milestone closed

The previous checkpoint captured the manifest bump; this one captures what landed after it.

- **Release:** https://github.com/anortham/razorback/releases/tag/v0.27.0 — published via `bump-version.sh --release`; the release-commit body is the changelog (security pass, security lane, claude reviewer allowlist fix)
- **State:** main @ 18f3034 (`release: 0.27.0 security lane and pre-merge security pass`), tag `v0.27.0` pushed with `--follow-tags`, tree clean
- **Cleanup:** the `security-pass` worktree and `worktree-security-pass` branch are removed; `git branch -d` confirmed the merge before deletion
- **Verified:** npm test 196/196 on merged main, `bump-version.sh --audit` clean, tagged-commit manifest check passed during `--release`

**Impact:** PRs #8–#10 are now installable at 0.27.0 — the security features the hospital convention needs (External model policy block, security scan scopes, two-pass pre-merge review) are shipped.

**Open follow-ups** (tracked in `.memories/autonomous-run-2026-08-07-security-pass.md`): isolated credential-free reviewer checkout + malicious-diff fixtures; verify a claude `--safe-mode`-style flag before adopting; add the External model policy block to razorback's own CLAUDE.md; CI/DevOps PR reviewer for the hospital; stale Goldfish brief "Harness descope + 2026-07 audit remediation" still needs a decision (complete or update).
