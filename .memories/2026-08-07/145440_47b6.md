---
id: checkpoint_47b6d650
timestamp: 2026-08-07T14:54:40.134Z
tags:
  - comparative-audit
  - superpowers-sync
  - skills-audit
  - mattpocock-skills
  - brainstorming
git:
  branch: main
  commit: 411a2c2
summary: "Comparative audit: superpowers v6.2.0 + mattpocock/skills 1.2.3 vs
  razorback 0.27.0"
type: learning
impact: Ranked adoption backlog covering 5 live bugs and ~15 candidate
  adaptations from both upstream repos; skip list recorded to prevent
  re-litigation.
next: "User picks scope: quick-fix lane for the 5 bugs (fixing-small-issues
  tier), or brainstorm+plan a full adoption cycle (writing-good-tests, SDD
  fix-loop discipline, plan-scoped workspace, etc.), or both."
unknowns:
  - Cursor hook runner behavior with a shell key
  - no in-repo eval evidence for upstream's SDD fix-loop redesign
  - AI Coding Dictionary full term list not fetched
---

## Comparative audit: superpowers v6.2.0 + mattpocock/skills 1.2.3 vs razorback 0.27.0

**WHAT:** Completed the brief `harness-descope-2026-07-audit-remediation` (work shipped in v0.27.0), then ran a two-agent parallel examination of `~/source/superpowers` (HEAD 44c9b2d, 51 commits since the 2026-07-03 baseline) and `~/source/skills` (HEAD 84fdeff, 105 commits since the 2026-07-17 baseline) against razorback main (411a2c2).

**WHY:** Both upstream repos had months of unexamined activity; razorback periodically harvests ideas from them.

**HOW:** Two read-only Explore subagents, one per repo, each given the baseline date, prior-adoption history, and razorback's conventions; each verified razorback's current state with Miller/grep before proposing anything.

### Live razorback bugs found by comparison

1. `skills/systematic-debugging/find-polluter.sh:22-23` — original broken `find` logic (missing `./` prefix, empty-result counted as 1, `**/` never matches zero dirs). Upstream has verbatim fix + 90-line test suite.
2. `hooks/hooks.json` — both SessionStart AND SubagentStart entries lack `"shell": "bash"`; on Windows the bootstrap and the Miller-first subagent injection silently never load (PowerShell/CMD cannot parse the command). Safe additive fix; older Claude Code ignores the key. Cursor runner behavior with `shell` key unverified.
3. `finishing-a-development-branch` Interactive Step 5 greps the worktree by `git branch --show-current` AFTER Options 1/4 checked out the base branch → cleanup silently no-ops. Fix shape: capture WORKTREE_PATH + feature branch up front (superpowers 0b47219).
4. `skills/systematic-debugging/SKILL.md:92-104` — instrumentation example runs `env | grep IDENTITY`, printing a credential into agent-visible output; razorback has ZERO redaction guidance anywhere despite four external-model delegation skills.
5. Harness-specific tool names in subagent-consumed prompt templates: `implementer-prompt.md:6`, `brainstorming/spec-document-reviewer-prompt.md:10`, `writing-plans/plan-document-reviewer-prompt.md:10`, `brainstorming/SKILL.md:156`, `pre-merge-review/fix-dispatch-prompt.md:3` all say `Agent tool (general-purpose)` etc. outside `<!-- harness -->` guards.

### Top adoption candidates (merged ranking)

- **writing-good-tests.md** (superpowers): falsifiability, change-detector ban, mirror-assertion ban, mutation check; replaces razorback's old 299-line testing-anti-patterns.md; broaden TDD trigger to any test writing. Also indicts razorback's own string-presence tests → pair with region-scoped assertions (a60dc2f technique).
- **Redact rule**: 3-sentence canonical block in security-review + verbatim copies via security-checklist-sync test; point systematic-debugging at it.
- **SDD fix-loop discipline** (adapt to inline-review model, NOT as subagent files): scoped re-review contract (FIX_BASE = head previous review saw; per-finding ADDRESSED/NOT ADDRESSED; fix-diff-only; out-of-scope sink), fix-report completeness gate, adjudication ritual at cap with load-bearing check (maps to blocker taxonomy #3), minor-finding deferral handed to pre-merge-review/harvesting-debt. Keep razorback's 4-attempt reframed cap — better than upstream's 5-round capability bump. Note: upstream shipped no in-repo eval evidence for the fix loop (unlike plan-scoped workspace, which has committed RED/GREEN results).
- **Plan-scoped SDD workspace** `.razorback/sdd/<plan>/`: adopt for contamination/token reasons; upstream eval honest — foreign-ledger adoption did NOT reproduce (25/25 refused), win is a 6-13-tool-call forensics tax + hygiene. Preserve parallel-lead-commit crash-window rule when adding identity header.
- **Brief-vs-paste contradiction**: implementer-prompt.md says paste full task text; SKILL.md File Handoffs says point at brief. Resolve in favor of brief path; exact values live only in the brief; record BASE via `git rev-parse HEAD` before dispatch.
- **Forge-agnostic PR ladder**: gh → other forge CLI → push-printed URL → Status: Partial. Razorback's final stop is PR creation and is gh-hardcoded today.
- **Compression sweep** with measured caveat: naive deletion of argument-bearing prose degraded probes 8/10→5/10; rule = delete sales prose, compress rebuttals into rows carrying the full argument. Razorback carries pre-sweep text in ~12 named locations; its TDD table is the old terse form. Keep razorback's Integration sections (they carry routing logic).
- **writing-skills additions** (mattpocock): the no-op test (model-relative, "settle by running the document"), "prompt the positive" scoped so anti-rationalization negations stay (earned by observed RED-run rationalization), invocation-axis as a recorded decision, "It's working if" + defining-constraint in the body template.
- **brainstorming partial**: frontier-empty stop condition + "finding facts is the agent's job — dispatch a subagent, don't ask the user". Keep one-question-per-message and falsifiable-guess (stronger than upstream).
- **wizard skill** (biggest new capability, biggest build): generates a bash script walking a human through credential/env setup; converts blocker taxonomy #1 stops into a morning-report deliverable. template.sh ports as-is.
- **HTML prototype branch**: single-file HTML shell + tabbed guided walkthroughs as a second branch in prototyping/LOGIC.md, selected by who renders the verdict.
- **Cheap extras**: `.out-of-scope/` decision records (why + escape hatches + prior requests), router-freshness invariant as a test, RELEASE-NOTES.md in problem→evidence→mechanism voice, ADR three-part gate; glossary layer still deferred but "read CONTEXT.md if it exists" one-liner gets 80%.

### Deliberate skips (recorded so nobody re-proposes)

Five-round breaker count; re-review/task-reviewer prompt files as dispatched subagents (violates inline review); model-selection table; Gemini restore (upstream reverted, razorback's drop stands); bucket folders + in-progress channel (ADR 0002 confirms flat skills/ is load-bearing for Codex `"skills": "./skills/"`); changesets/version-sync (bump-version.sh strictly ahead); emoji question format; docs-page/aihero publishing apparatus; Common-questions section (no evidence corpus).

**IMPACT:** Full ranked backlog for the next razorback improvement cycle. Two full reports live in this session's transcript; this checkpoint is the durable summary.
