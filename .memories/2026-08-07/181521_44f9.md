---
id: checkpoint_44f97931
timestamp: 2026-08-07T18:15:21.239Z
tags:
  - razorback
  - release
  - 0.28.0
  - milestone
  - version-bump
  - github-release
git:
  branch: main
  commit: 26e73da
summary: v0.28.0 published — release cycle closed
next: "Nothing in flight. Next candidates: the flagged workspace-key decision or
  the redaction-preflight follow-up."
---

## v0.28.0 published — release cycle closed

The release completed after checkpoint_f20943f5 (which captured the merge and bump):

- **Release commit:** 26e73da `release: 0.28.0 upstream adoption, SDD hardening, and the visual digest` — its body is the changelog and became the GitHub release notes.
- **Tag:** v0.28.0 (annotated), pushed with `git push --follow-tags`.
- **Published:** https://github.com/anortham/razorback/releases/tag/v0.28.0 via `./scripts/bump-version.sh --release`, which verified the tagged commit's manifests declare 0.28.0.
- **Final state:** main clean at 26e73da, in sync with origin; no worktrees; the feature branch is deleted locally and on the remote.

**Why 0.28.0:** minor bump — the plan added features (visual digest, SDD process upgrades, upstream adoptions) plus the five audit bug fixes and the codex-review security fixes.

**What the next session should know:**
- Open follow-ups live in `.memories/autonomous-run-2026-08-07-upstream-adoption.md` → Next steps: the flagged workspace-key decision (basename vs path-derived slug), the outbound-delegation redaction preflight, the reviewer-checklist mutation bullet, cap-adjudication pointers, and the spec-reviewer prompt's brief-path wording.
- Future SDD runs use the new plan-scoped workspace layout (`.razorback/sdd/<plan-basename>/`) and the brief-path dispatch contract — the scripts enforce the new signatures.
