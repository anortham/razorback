---
id: checkpoint_21da609a
timestamp: 2026-08-07T12:10:39.996Z
tags:
  - doc-drift
  - pre-merge-review
  - claude-cli
  - guard-tests
  - tdd
git:
  branch: main
  commit: edc714b
summary: Fixed claude reviewer allowlist drift + added guard tests
briefId: harness-descope-2026-07-audit-remediation
next: Commit (serial-worker-commit), then razorback:pre-merge-review with codex,
  then finishing-a-development-branch.
---

## Fixed claude reviewer allowlist drift + added guard tests

**WHAT:** On branch `worktree-claude-reviewer-allowlist` (plan `docs/plans/2026-08-07-claude-reviewer-allowlist-drift.md`, approved with codex review):
- `skills/pre-merge-review/SKILL.md:109` — claude reviewer flag summary now `--tools "Read,Grep,Glob" --strict-mcp-config` (was `Read,Bash`, no strict-mcp-config); removed false "self-contained/inlined" schema claims (line 111 too).
- `skills/claude-cli/adversarial-prompt.txt` — REVIEW METHOD now "Investigate read-only with the Read, Grep, and Glob tools." (was instructing Bash, which the allowlist blocks).
- `skills/claude-cli/SKILL.md:286` + `skills/pre-merge-review/reviewer-prompts/codex.md:115` — divergence descriptions updated from `Read`/`Bash` to `Read`/`Grep`/`Glob`.
- Extended `tests/claude-cli-docs.test.mjs` (bash-block allowlist guard; Read,Bash only in the anti-pattern warning) and created `tests/adversarial-prompt-sync.test.mjs` (trio shared-section byte-sync, opening line, placeholder order, claude tool-sentence guard).

**WHY:** 2026-08-07 assessment (checkpoint_4aceb150) found the drift: docs described a write-capable "read-only" reviewer under --dangerously-skip-permissions. The 2026-07-26 incident proposed exactly this guard class.

**HOW:** TDD — guards written first, red confirmed (8 pass / 2 fail, exactly the two defect guards), doc edits applied, green 10/10.

**Verification ledger:**
- worker red: `node --test tests/claude-cli-docs.test.mjs tests/adversarial-prompt-sync.test.mjs` — 2 expected failures (Read,Bash guard, tool-sentence guard) — pre-fix tree
- worker green: same command — 10/10 pass — post-fix tree
- branch-gate: `npm test` 175/175 + `./scripts/bump-version.sh --audit` clean at 0.26.2 — post-fix tree, pre-commit (2026-08-07)

**IMPACT:** The read-only claim for the claude pre-merge reviewer is now consistent everywhere and CI-guarded. Next: commit, codex pre-merge review, finishing-a-development-branch (push + PR).
