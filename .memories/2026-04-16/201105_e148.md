---
id: checkpoint_e14859c9
timestamp: 2026-04-16T20:11:05.193Z
tags:
  - harness-support
  - feature-complete
  - merged-to-main
git:
  branch: main
  commit: a87d338
summary: "Harness-friendliness pass: merged to main"
type: checkpoint
impact: Razorback now installs and runs on six harnesses (up from three).
  Cross-harness code-reviewer gap closed. Hook/plugin failure modes are audible.
  Version drift across five manifests is now caught by --check/--audit.
next: "Optional: push main to origin; sweep manifest descriptions; address
  subagent Julie inheritance as a separate pass"
---

## Harness-friendliness pass: merged to main

### What shipped (15 commits, 594 insertions / 112 deletions across 17 files)

**New harness support:**
- Cursor: `.cursor-plugin/plugin.json` manifest
- Gemini CLI: `gemini-extension.json` + `GEMINI.md` (includes using-razorback + gemini-tools via `@./` syntax)
- Copilot CLI: `skills/using-razorback/references/copilot-tools.md` (async bash, sql todos, plugin-agent discovery)
- Gemini CLI: `skills/using-razorback/references/gemini-tools.md` (no-subagent → executing-plans fallback)

**Bootstrap skill (using-razorback/SKILL.md):**
- `<SUBAGENT-STOP>` block at top prevents bootstrap re-injection inside subagents
- "Instruction Priority" section (user > skill > default)
- "How to Access Skills" now lists all six harnesses
- "Platform Adaptation" references all four tool-mapping sources

**Cross-harness review gap fixed:**
- `requesting-code-review/SKILL.md` Mode 2 has a six-harness fallback table with explicit dispatch instructions; Codex/OpenCode get a two-file concatenation recipe (agents/code-reviewer.md system prompt + requesting-code-review/code-reviewer.md filled template)
- `code-quality-reviewer-prompt.md` reframed as inline checklist, no longer implies a Claude-Code-only dispatch

**Failure-mode hardening:**
- `run-hook.cmd` emits stderr warning on Windows-without-bash (was silent)
- `.opencode/plugins/razorback.js`: console.warn on regex-replace miss, try/catch around messages.transform, comment warning about `experimental.*` prefix rename risk

**Tooling:**
- `scripts/bump-version.sh` + `.version-bump.json` ported from superpowers (verbatim script, razorback-specific file list). `--check` / `--audit` / `<new-version>` keep all five manifests in sync
- `.claude-plugin/marketplace.json` version synced 0.7.4 → 0.8.0
- `.gitignore` += `.julie/`
- `CLAUDE_MD_TESTING.md` variants generalized to harness-agnostic phrasing

**Docs:**
- README Installation covers six harnesses with per-harness install one-liners; Updating section covers all six
- CLAUDE.md harness split is a six-row table; bootstrap mechanics section enumerates all six paths

### Process notes

- Team-driven execution: three teammates (A/B/C) worked in parallel on ~12 tasks, lead handled L1. Zero file-ownership conflicts. One real fix surfaced during review: teammate-c caught that bump-version.sh's audit excludes match basenames not paths, corrected `docs/plans` → `plans` in the exclude list.
- Subagents don't inherit parent's Julie MCP config — teammates fell back to Read-based orientation. Flagged as a separate follow-up; not a regression.
- Single-repo marketplace layout (Claude Code + Copilot CLI both read `.claude-plugin/marketplace.json`) stays for now; separate marketplace repo was the documented fallback but wasn't needed.

### State

- Main at `a87d338`, 17 commits ahead of `origin/main` (2 pre-feature + 15 feature). Not pushed.
- Feature branch deleted, worktree removed.
- Design + implementation plan preserved at `docs/plans/2026-04-16-harness-friendliness{-design,}.md`.

### Known follow-ups (not blocking)

- Cursor marketplace submission
- Mode 2 lead-in phrasing in requesting-code-review still Claude-Code-centric (the table below resolves it)
- Manifest descriptions still say "Claude Code, Codex, OpenCode" (pre-dates the six-harness expansion) — sweep when convenient
- Subagent Julie inheritance (open question: worth adding ToolSearch call guidance to implementer prompt template?)
