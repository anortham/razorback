---
id: checkpoint_c37bc4f5
timestamp: 2026-09-24T14:02:13.678Z
tags:
  - workflow-reduction
  - baseline
  - experiment
git:
  branch: main
  commit: 6e56b6c
  files:
    - TODO.md
    - docs/plans/2026-09-24-workflow-comparison.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Pinned the baseline for the workflow comparison
briefId: inner-loop-test-isolation
type: decision
decision: Use v0.44.4 as the current-workflow baseline and keep the lighter
  default on an unmerged branch until the comparison finishes.
alternatives:
  - "Use HEAD 0ed1f7b as the baseline: rejected because it is not a release tag,
    although the workflow files are identical"
  - "Merge the reduction before the comparison: rejected because the rollout
    rule requires outcome evidence first"
next: Create a worktree for the workflow reduction and start the item that
  removes automatic process escalation
---

## Pinned the baseline for the workflow comparison

- **WHAT:** Added `docs/plans/2026-09-24-workflow-comparison.md`. It pins tag `v0.44.4` (commit `07335cf`) as the current-workflow arm. Checked off the baseline item in `TODO.md` under "Default workflow reduction".
- **WHY:** The comparison item needs a current workflow that stays reproducible after the skill defaults change. The baseline must be recorded before the first skill edit.
- **HOW:** Checked `git diff --stat v0.44.4 HEAD`. Only `docs/site/`, its test, one `CLAUDE.md` line, and `.memories/` changed after the tag, so `v0.44.4` is the same workflow as HEAD. The notes file holds empty tables for the pinned run inputs and for results. It lists the task classes and measures from TODO.
- **IMPACT:** Unblocks the workflow reduction items. Agreed order: build the lighter default on one branch without a merge, compare it against `v0.44.4` (start with existing transcripts, which cost nothing), then merge only the changes that the results support. Paid replays need a budget from the user.
