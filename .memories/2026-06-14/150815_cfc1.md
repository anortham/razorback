---
id: checkpoint_cfc16f2b
timestamp: 2026-06-14T15:08:15.763Z
tags:
  - release
  - razorback
  - autonomous-execution
  - skills
  - version-bump
  - v0.17.2
git:
  branch: main
  commit: ebf0ee0
  files:
    - .claude-plugin/marketplace.json
    - .claude-plugin/plugin.json
    - .cursor-plugin/plugin.json
    - gemini-extension.json
    - package.json
    - skills/test-driven-development/SKILL.md
    - tests/autonomous-process-gates.test.mjs
summary: Prepared Razorback v0.17.2 autonomy wording release
briefId: agent-tier-delegation-gate-policy-feedback
type: checkpoint
impact: Unblocks publishing the autonomy wording fix as Razorback v0.17.2.
evidence:
  - "node --test tests/*.test.mjs: 41 pass, 0 fail"
  - "git diff --check: exit 0"
  - "./scripts/bump-version.sh --audit: all declared files in sync at 0.17.2"
next: Commit the release changes, create tag v0.17.2, push main and the tag.
---

## Prepared Razorback v0.17.2 autonomy wording release

The skill review found one unscoped TDD permission-stop sentence that could interfere with long-running autonomous runs.

- **WHAT:** Updated `skills/test-driven-development/SKILL.md` so exceptions are limited to explicit user-request or approved-plan exceptions and missing exception language routes through TDD/blocker taxonomy instead of a mid-run permission stop.
- **WHY:** Razorback should encourage autonomous agent work after approval; unscoped permission language can wake the user unnecessarily during long-running sessions.
- **HOW:** Added a regression assertion in `tests/autonomous-process-gates.test.mjs`, verified it failed on the old sentence, then changed the skill wording and bumped all version-bearing manifests to `0.17.2` with `scripts/bump-version.sh`.
- **IMPACT:** The autonomous process gate now protects against this wording drift, and the release tree is ready to commit, tag, and push as `v0.17.2`.

Verification before commit:
- `node --test tests/*.test.mjs`: 41 pass, 0 fail
- `git diff --check`: exit 0
- `./scripts/bump-version.sh --audit`: all declared files in sync at `0.17.2`, no undeclared references
