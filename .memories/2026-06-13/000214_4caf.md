---
id: checkpoint_4caf1e02
timestamp: 2026-06-13T00:02:14.246Z
tags:
  - skills
  - cross-model-convergence
  - harness-neutral
  - cursor-agent
  - codex
git:
  branch: main
  commit: 2e8c206
  files:
    - README.md
    - skills/architecture-quality/SKILL.md
    - skills/architecture-quality/analysis-heuristics.md
    - skills/architecture-quality/architecture-language.md
    - skills/cross-model-convergence/SKILL.md
    - skills/subagent-driven-development/SKILL.md
    - tests/cursor-agent-skill.test.mjs
summary: Made cross-model-convergence harness-neutral + audited all skills for
  harness assumptions
briefId: agent-tier-delegation-gate-policy-feedback
type: checkpoint
next: Uncommitted on main; user review then release bump.
---

## Made cross-model-convergence harness-neutral + audited all skills for harness assumptions

User wants razorback skills usable from any lead harness (e.g. Codex lead with cursor-agent reviewer; Codex has a `goal` command as its until-condition driver).

- **Full-skill audit** (Explore agent, all skills/ except intentionally model-specific cursor-agent/codex-cli/gemini-cli/claude-cli/using-razorback): 13/15 neutral skills already clean. Two files needed fixes.
- **cross-model-convergence rewritten lead-relative:** "the lead — whichever agent and harness is running this skill"; reviewer = any external CLI whose *model differs from the lead* (self-review via your own model's CLI never counts); Claude lead defaults codex-cli, Codex lead picks claude/gemini/cursor-agent. cursor-agent reviews read-only (`--mode ask` + `--trust`, never `--force`), diff-check after each reviewer call (revert reviewer edits, doesn't consume cap), stateless channels get full context re-supplied (session resume via `create-chat`/`--resume` when available). **Goal predicate bug fixed:** terminal state now includes the round cap as exit (b) — success-only predicate loops forever on non-converging codebases. Goal-Driver section names `/loop` (Claude Code) and `goal` (Codex); round checkpoints now include the reviewer session handle; CONVERGENCE STATUS printed + checkpointed, driver keys on `clean_round: yes` or `round: cap/cap`.
- **subagent-driven-development:** removed hardcoded `--model composer-2.5-fast` (model pinning lives only in cursor-agent skill); test flipped to `doesNotMatch` to enforce the no-duplicated-model-names policy (tests/cursor-agent-skill.test.mjs).
- **Verified:** RED test (GPT lead in Codex, old skill) showed role confusion + the predicate gap; GREEN test (updated skill) ran the pairing correctly end-to-end including mid-loop goal-rerun recovery via Goldfish recall. Suite 40/40.
