---
id: checkpoint_db8ecd56
timestamp: 2026-07-14T22:59:47.983Z
tags:
  - codex-review
  - review-fixes
  - cli-verification
git:
  branch: fix/review-findings-2026-07-14
  commit: e64f009
  files:
    - skills/brainstorming/SKILL.md
    - skills/claude-cli/SKILL.md
    - skills/codex-cli/SKILL.md
    - skills/dispatching-parallel-agents/SKILL.md
    - skills/finishing-a-development-branch/SKILL.md
    - skills/finishing-a-development-branch/morning-report-template.md
    - skills/gemini-cli/SKILL.md
    - skills/pre-merge-review/SKILL.md
    - skills/pre-merge-review/reviewer-prompts/claude.md
    - skills/pre-merge-review/reviewer-prompts/codex.md
    - skills/pre-merge-review/reviewer-prompts/gemini.md
    - skills/requesting-code-review/SKILL.md
    - skills/subagent-driven-development/SKILL.md
    - skills/subagent-driven-development/fix-prompt.md
    - skills/systematic-debugging/condition-based-waiting.md
    - skills/systematic-debugging/root-cause-tracing.md
    - skills/using-razorback/references/codex-tools.md
summary: "Codex review of fix branch: 13/13 findings verified, all fixed"
briefId: agent-tier-delegation-gate-policy-feedback
type: checkpoint
evidence:
  - "claude 2.1.209: $schema rejection reproduced; --tools Read,Grep,Glob
    verified; --strict-mcp-config exists"
  - "codex 0.144.3: collaboration tools enumerated live; review flag placement
    exit codes 0/2"
  - "jq: empty findings[] exit 4 reproduced"
  - "npm test: 111/111 pass"
---

## Codex review of fix branch: 13/13 findings verified, all fixed

**WHAT:** Ran the codex-cli adversarial review of `fix/review-findings-2026-07-14` vs main. All 13 findings survived verification against installed CLIs and files — none refuted. Fixed everything in a follow-up pass.

**Key fixes:**
- **`$schema` key breaks claude `--json-schema`** (verified live: `no schema with key or ref` error) — stripped from claude-side schema strings in claude-cli SKILL + pre-merge claude.md.
- **`--tools "Read,Bash"` is not read-only** (Bash writes; MCP tools also leak in — verified) — reviews now pin `--tools "Read,Grep,Glob" --strict-mcp-config`.
- **`jq -e '.findings[]'` exits 4 on a valid empty array** — clean reviews read as parse failures. All parse sites now shape-check `.findings | type == "array"` then iterate `.findings[]?`.
- **Codex collaboration surface drift** (verified live on 0.144.3): `spawn_agent(task_name,…)`, `followup_task`, `send_message`, `wait_agent(timeout_ms)`, `interrupt_agent`, `list_agents`; no `send_input`/`close_agent`/`agent_type`. Updated codex-tools.md, SDD, fix-prompt, dispatching-parallel-agents, requesting-code-review.
- **`codex exec review` flag placement**: exec-level flags (`-C`, `-s`, `--color`) must precede `review` (verified); review DOES support `--output-schema` — recipe + quick-ref corrected.
- **Gemini pre-merge prompt never included the diff**: template had no `{{REVIEW_INPUT}}` placeholder, so the substitution was a no-op; also bash patsub `&` hazard (5.2+) and literal-quote hazard (3.2). Rebuilt as plain concatenation.
- **finishing-a-development-branch**: base-branch resolution now PLAN_BASE → origin/HEAD → main/master; `{{pr_url}}` rendered as pending then written back in new Step 7 (commit+push); metadata-only-commit evidence rule stated; stale Step 5 ref fixed.
- **brainstorming**: design doc no longer committed on the current branch (often main) — written uncommitted, moved into the worktree, committed there as first commit.
- Also: gemini review recipes now carry `--approval-mode plan`; `gemini --version` ≠ auth check; `claude login` → `claude auth login`; stdin redirects added to pre-merge claude/gemini invocations; EOF blank lines fixed.

**IMPACT:** npm test 111/111, `git diff --check` clean. Branch still unpushed; PR awaits user approval.
