---
id: checkpoint_08299244
timestamp: 2026-07-16T14:48:09.088Z
tags:
  - audit-remediation
  - pre-merge-review
  - codex
git:
  branch: worktree-audit-fixes-2026-07
  commit: 7baa326
summary: Pre-merge codex review complete — 3 findings, all fixed, branch gate re-green
briefId: harness-descope-2026-07-audit-remediation
type: checkpoint
---

## Pre-merge codex review complete — 3 findings, all fixed, branch gate re-green

Codex adversarial review of the full branch diff (80 files, main..HEAD) returned verdict `needs-attention` with 3 findings. All verified real, all fixed by parallel fix workers:

- **F1 high** (957ba67): bash patsub_replacement corruption in codex-cli's adversarial-prompt construction — `&` in diffs expanded to the matched placeholder on bash ≥5.2. Fixed with placeholder-split (%%/#) concatenation; new guard test proves byte-preservation against hostile input, red-green demonstrated on real bash 5.3.
- **F2 medium** (7cbf39c): Miller canon taught `inspect(path)`/`inspect(symbol,...)` but the live schema requires `target`. Fixed across canonical + SKILL.md + subagent-toolchain + all 5 host copies + 2 test locks + architecture-quality prose hints (lead residue fix).
- **F3 medium** (7baa326): reviewer-prompts anchored on undefined `$RAZORBACK_DIR`. Fixed to the `$SKILL_DIR` convention; new guard test resolves every referenced asset and rejects RAZORBACK_DIR reintroduction.

**Codex's 18-test-failure note dismissed:** its own read-only sandbox couldn't create temp dirs; local suite is green.

**Branch gate re-run at 7baa326:** npm test 152/152 (141 original + 11 new guard tests), rule copies clean, manifests in sync at 0.22.0, tree clean.

**Machine side effect to surface:** fix-f1 installed Homebrew bash 5.3 for the CI-matching red demo; nothing depends on it; `brew uninstall bash` reverses it.

**Next:** finishing-a-development-branch → PR creation (final stop).
